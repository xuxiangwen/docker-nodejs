it's a simple nodejs express sample from https://expressjs.com/en/starter/generator.html

   install dependencies:
     $ cd express_generator_sample && npm install

   run the app:
     $ DEBUG=express-generator-sample:* PORT=3002 npm start
